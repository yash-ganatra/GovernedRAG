"""
core — Embedding pipeline, vector store, and logging infrastructure.

Submodules:
    core.inference_logger  — InferenceLogger (SQLite audit log)
    core.audit_logger      — AuditLogger (embedding pipeline audit)
    core.vector_store      — VectorStoreManager (ChromaDB)
    core.document_loader   — Markdown / YAML loaders
    core.chunker           — Document chunking strategies
    core.traceability      — Traceability validation
"""
